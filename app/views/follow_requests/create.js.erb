var current_followers = $(".<%= dom_id(@follow_request.recipient) %>");

var updated_followers = $("<%= j(render 'follow_requests/follow_unfollow', sender: @follow_request.sender, recipient: @follow_request.recipient) %>");

current_followers.replaceWith(updated_followers); 
